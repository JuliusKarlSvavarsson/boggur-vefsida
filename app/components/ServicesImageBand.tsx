import ArchitecturalImageBand from "./ArchitecturalImageBand";

export default function ServicesImageBand() {
  // Reuse the existing architectural image band with the same smooth
  // "window pan" behaviour as á forsíðunni.
  return <ArchitecturalImageBand />;
}
